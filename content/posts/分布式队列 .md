---
title: "分布式队列"
date: 2024-10-10
tags: ["大数据技术基础"]
categories: ["Basic course"]
toc:
  enable: true
description: 
draft: false

---

## 第二章 分布式消息队列
### Kafka
#### Kafka 概述
0、数据会越来越多，此时可能需要增加多条数据线，每条数据线将收集到的数据导入不同的存储和分析系统。若仍采用之前的数据收集模式，会有以下潜在问题：
- 数据生产者和消费者耦合度过高
- 生产者和消费者间数据处理速率不对等
- 大量并发的网络连接对后端消费者不够友好

1、为降低数据生产者（比如Web Server）与消费者（比如Hodoop集群、实时监控系统等）之间的耦合性，使系统更易扩展，需引入一层“中间件”。
可以下几个角度理解Kafka的重要性：
- **消息中间件**：避免生产者和消费者直接互通产生的彼此高度依赖，两者中任何一个有变化都不会影响另一方。
- **消息队列**：缓存生产者产生的数据，使得消费者可以重复消费历史数据（比如数据处理后发现结果存在问题，需要重新读取数据进行处理）；平滑生产者产生数据速度和消费者处理数据速度的不对等。
- **发布订阅系统**：消费者可订阅某类主题的数据，当生产者产生对应主题的数据后，所有订阅者会快速获取到数据，即消费者可快速获取新增数据。另外，可随时增加新的消费者而无需进行任何系统层面的修改。
- **消息总线**：所有收集到的数据会流经 Kafka,之后由Kafka 分流后，进入各个消费者系统。

2、Kafka 特点：
高吞吐量、良好的扩展性、持久性和可靠性

3、Flume 与 Kafka 的区别

#### Kafka 设计架构
1、Kafka 是一个分布式消息队列，它将数据分区保存，并将每个分区保存成多份以提高数据可靠性。
- Kafka架构由***Producer、Broker和Consumer***三类组件构成，其中Producer将数据写入Broker，Consumer 则从Broker上读取数据进行处理；Broker构成了连接Producer和Consumer的“缓冲区”，Broker和Consumer通过ZooKeeper做协调和服务发现。

- 多个Broker构成一个**可靠**的分布式消息存储系统，避免数据丢失。Broker中的消息被划分成若干个topic，同属一个topic的所有数据按照某种策略被分成多个partition，以实现负载分摊和数据并行处理。

2、Kafka采用了不同于其他消息队列的 push-push 架构，而是采用了 push-pull 架构， 即Producer将数据直接“push”给Broker，而Consumer从Broker端“pull”数据，这种架构优势主要体现在以下两点:
- Consumer可根据自己的实际负载和需求获取数据，避免采用“push”方式给Consumer带来较大压力。
- Consumer自己维护已读取消息的offset而不是由Broker端维护，这大大缓解了Broker的压力，使得它更加轻量级。

#### Kafka程序设计

### Kafka典型应用场景



### 日志收集与分析-ELK Stack
1、ELK Stack架构
- E代表**ElasticSearch**：搜索和索引(Data Process)
- L代表**LogStash**：收集日志和事件数据，甚至可以解析和转换数据(Storage)
- K代表**Kibana**：可视化和共享(Visualize)
- 稍微复杂一些的ELK Stack架构包含了**beats**，这是一个轻量级的数据采集平台，集合了多种单一用途的数据采集器，可以应对更多样的数据收集和传输任务，具有以下特性：
  - 跟踪数据沿袭：可以从服务器环境中收集日志和指标，对必要元数据进行记录后传输人到ELK Stack中；
  - 可扩展：允许用户自行构建监督某个专用协议的工具。
- 当处理规模非常庞大的数据时，还可以加入**RabbitMQ**和**Kafka以**增加数据传输过程中的缓冲和弹性。
- 同时还可以加入**Nginx**来保证数据的安全性。

2、ELK应用
日志会记录一些比较重要的信息，如应用程序的log记录的error，warn级别的log，ELK就是处理日志信息高手。
如果线上环境出了故障，ELK能够立刻准确定位。ELK也经常在运维工作中大放光彩，在应用级别的实时监控，非常适合一些重要核心服务的预警。

#### ElasticSearch
1、Elasticsearch介绍
- 一个NoSQL数据库。它基于Lucene搜索引擎，并使用RESTful API构建。它提供简单的部署，可靠性和易管理性，还提供高级查询以执行详细分析并集中存储所有数据
- Elasticsearch允许**存储**，**搜索**和**分析**大量数据。它主要用做底层引擎，为完成搜索要求的应用程序提供支持。
- Elasticsearch具备以下特性：
  - 开源搜索服务器使用Java编写
  - 可用于索引任何类型的异构数据
  - 具备带有JSON输出的REST API Web界面
  - 全文检索
  - 近实时（NRT）搜索，实时分析
  - 具备可搜索的JSON文档存储，并允许分片和复制
  - 基于REST和JSON的模式自由（schema-free）分布式文档存储
  - 多语言和地理位置支持

2、Elasticsearch中比较重要的概念
| 概念 | 解释 |
| ---- | ---- |
| Cluster（集群）| 集群是节点的集合，它们共同保存数据并提供连接的索引和搜索功能。 |
| Type（类型）| 每个索引里都可以有一个或多个 Type，Type 是索引中的一个逻辑数据分类，一个 Type 下的文档，都有相同的字段（Field）。 |
| Node（节点）| 节点表示弹性搜索实例，它是在弹性搜索实例开始时创建的。 |
| Index（索引）| 索引是具有类似特征的文档集合，如客户数据、产品目录，在执行索引、搜索、更新和删除操作时有用，一个集群中可以定义多个索引。 |
| Document（文档）| 文档是可以被索引的基本信息单位，以JSON键值对表示，如{“user”:“nullcon”},每个文档都与一个类型和一个唯一ID相关联。 |
| Shard（分片） |每个索引都可以拆分为多个分片，以便能够分发数据。分片是索引的原子部分，如果要添加更多节点，则可以将其分发到集群上 |


3、ElasticSearch特性：
优点：
- 天生分片、天生集群
- 天生索引（空间换时间）
- 支持PB级海量数据实时全文搜索
- 支持多语言访问，支持TCP和RESTful API两种方式访问

缺点：
- 不合适做复杂聚合，会影响ES集群的性能
- 不支持高并发写入数据
- ES耗CPU和内存资源，需要用高配置的机器来搭建集群，成本高

4、ElasticSearch应用场景
- 日志分析
- 信息检索
- 数据智能

#### Logstash
1、Logstash 是一个轻量级、开源的日志收集处理框架，可以方便的把分散、多样化的日志搜集起来，并进行自定义过滤分析处理，然后传输到指定位置（如送入 Elasticsearch）
Logstash 使用 JRuby 语言编写；
Logstash 可以统一来自不同来源的数据，并将数据标准化为想要的格式；允许清理和本地化所有数据，以便对用例进行分析和可视化。

2、Logstash 的设计简单，其功能由三部分组成：
 Input：数据收集
 Filter：数据加工，如过滤，改写等
 Output：数据输出

3、Logstash 的特点
- 使用内部队列将事件传递到每个阶段
- 允许日志的不同输入
- 过滤/解析日志

4、Logstash 的优点
- 几乎可以访问任何数据
- 可以和多种外部应用整合
- 支持动态、弹性拓展

5、Logstash 运行逻辑
- **Shipper**: 主要用来收集日志数据，负责监控本地日志文件的变化，及时把日志文件的最新内容收集起来，然后经过加工、过滤，输出到 Broker。
- **Broker**: 相当于日志Hub，用来连接多个 Shipper 和多个 Indexer。
- **Indexer**: 从 Broker 读取文本，经过加工、过滤，输出到指定的介质 (可以是文件、网络、elasticsearch 等) 中。

#### Kibana
1、Kibana 是一个开源的数据分析可视化平台，可以为 Logstash 和 ElasticSearch 提供的日志数据进行高效的搜索、可视化汇总和多维度分析，还可以与 Elasticsearch 搜索引擎之中的数据进行交互。
它基于浏览器的界面操作可以快速创建动态仪表板，实时监控ElasticSearch的数据状态与更改。
在Kibana中，有不同的方法可以对数据执行搜索。 以下是最常见的搜索类型：
| 搜索类型 | 用法 |
| ------- | -- |
| 自由文本搜索 | 用于搜索特定字符串 |
| 字段级搜索 | 用于搜索特定字段中的字符串 |
| 逻辑语句 | 用于将搜索组合成逻辑语句 |
| 近似搜索 | 用于搜索特定字符的近似项 |

2、Kinbana 的优点：
- 强大的前端仪表板，能够可视化集群的索引信息
- 实现索引信息的实时搜索
- 可以搜索，查看和与 Elasticsearch 中存储的数据进行交互
- 对数据执行查询并可视化图表，表格和映射中的结果
- 可配置的仪表板，用于在 elasticsearch 中对日志记录日志进行切片和切块
- 能够以图表，图表等形式提供历史数据
- 实时仪表板，可轻松配置
- 实现索引信息的实时搜索


### 商业化大数据解决方案-Splunk
1、Splunk是什么？
- 面向机器数据的全文搜索引擎（使用搜索引擎的方式处理数据；支持海量级数据处理）
- 准实时的日志处理平台
- 基于时间序列的索引器
- 大数据分析平台

2、Splunk特性
- 数据摄取（data ingestion）
- 数据索引（data indexing）
- 数据搜索（data searching）
- 使用警报（using alert）
- 仪表盘（dashboard）
- 数据模型（ data model）

3、Splunk应用场景
- IT运营
- SIME（Security Information and Management）
- 物联网

### 网络数据采集-爬虫
1、网络数据采集

2、爬虫：通过程序代码模拟浏览器向网站发起请求，把站点（服务器）返回的HTML代码/JSON数据/二进制数据（图片、视频） 爬取到本地，进而通过解析代码分析并提取自己需要的数据；（若将爬虫比喻为蜘蛛，互联网将是一张蜘蛛网）

3、网络爬虫原理
- 网络爬虫是一种按照一定的规则，自动地抓取web信息的程序或者脚本
- 网络爬虫可以自动采集所有其能够访问到的页面内容，为搜索引擎和大数据分析提供数据来源。
- 从功能上讲，爬虫一般数据**采集**，**处理**和**存储**三部分功能

4、网络爬虫原理
URL的抓取
网页的遍历
构造URL队列

5、网络爬虫工作流程
- 初始时将预先设定的种子URL放入待抓取URL队列。
- 从待抓取队列中取出URL，进行DNS解析并下载网页内容，然后放入已抓取URL队列。
- 从已抓取队列中抽取出新的URL放入待抓取队列，重复步骤二直到定停


### Scrapy 框架简介